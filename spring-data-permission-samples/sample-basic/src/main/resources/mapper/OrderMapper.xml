<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.github.steventsai.permission.sample.mapper.OrderMapper">

    <resultMap id="BaseResultMap" type="com.github.steventsai.permission.sample.entity.Order">
        <id column="id" property="id"/>
        <result column="order_no" property="orderNo"/>
        <result column="customer_name" property="customerName"/>
        <result column="status" property="status"/>
        <result column="org_id" property="orgId"/>
        <result column="owner_user_id" property="ownerUserId"/>
    </resultMap>

    <sql id="BaseColumnList">
        id, order_no, customer_name, status, org_id, owner_user_id
    </sql>

    <!-- No permission filtering (admin/internal use) -->
    <select id="selectAll" resultMap="BaseResultMap">
        SELECT <include refid="BaseColumnList"/>
        FROM biz_order
    </select>

    <!-- With permission filtering (user-facing) -->
    <select id="selectWithScope" resultMap="BaseResultMap">
        SELECT <include refid="BaseColumnList"/>
        FROM biz_order
        WHERE 1=1
        <include refid="com.github.steventsai.permission.ScopeCondition"/>
    </select>

    <!-- Count with permission filtering -->
    <select id="countWithScope" resultType="long">
        SELECT COUNT(*)
        FROM biz_order
        WHERE 1=1
        <include refid="com.github.steventsai.permission.ScopeCondition"/>
    </select>

    <select id="selectById" resultMap="BaseResultMap">
        SELECT <include refid="BaseColumnList"/>
        FROM biz_order
        WHERE id = #{id}
    </select>

    <insert id="insert" useGeneratedKeys="true" keyProperty="id">
        INSERT INTO biz_order (order_no, customer_name, status, org_id, owner_user_id)
        VALUES (#{orderNo}, #{customerName}, #{status}, #{orgId}, #{ownerUserId})
    </insert>

</mapper>
